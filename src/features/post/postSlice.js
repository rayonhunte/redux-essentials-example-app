import { createSlice } from '@reduxjs/tookkit';


const initialState = []


const postsSlice = createSlice({
    name: 'post',
    initialState,
    reducers: {}
})


expost default postSclie.reducers